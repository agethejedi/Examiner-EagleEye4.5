import { combineScores } from './riskEngine.js';
function basicAddrHeuristics(addr){const flags=[];let s=0;const a=addr.trim();if(/^(0x)?[a-fA-F0-9]{40}$/.test(a.replace(/^0x/,'')))flags.push({sev:'ok',msg:'Looks like a valid EVM-style address'});if(/^bc1[0-9a-z]{25,39}$/i.test(a))flags.push({sev:'ok',msg:'Bech32 SegWit format (BTC)'});if(a.length<10){s+=20;flags.push({sev:'warn',msg:'Address is very short'});}if(/(mix|tumb|privacy)/i.test(a)){s+=30;flags.push({sev:'danger',msg:'Name resembles mixing/tumbling'});}return {score:Math.min(100,s),flags};}
export async function analyzeCrypto({networks,addresses}){const flags=[];if(!addresses?.length){return {score:0,flags:[{sev:'warn',msg:'Provide at least one address'}]};}if(!networks?.length){return {score:0,flags:[{sev:'warn',msg:'Select at least one network'}]};}let parts=[];for(const addr of addresses){const h=basicAddrHeuristics(addr);parts.push({score:h.score,weight:1});h.flags.forEach(f=>flags.push(f));}if(networks.length>=8){flags.push({sev:'warn',msg:'Many networks selected — results are generalized'});parts.push({score:20,weight:0.5});}const score=combineScores(parts);return {score,flags};}
